# 开发者指南

- 开发环境与依赖管理（Go、GOPROXY、`go mod tidy`）
- 运行参数与配置：`-headless`、`-bin`、`-port`；`configs/*` 说明
- 日志规范：`logrus` 等级与字段建议
- 错误规范：统一错误码与消息字段（对齐 `handlers_api.go`）
- 目录结构与命名约定
- 代码风格与提交建议（后续可补充 lint/hook）
